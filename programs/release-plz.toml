[workspace]
publish = true
pr_branch_prefix = "release-plz-programs-"
pr_name = "chore: Release programs v{{ version }}"
git_tag_name = "{{ package }}-v{{ version }}"
git_release_name = "{{ package }}-v{{ version }}"
